import React, { useState } from 'react';
import { useDatabase } from '../App';

export const PatternSectionService = ({}) => {
    const { db } = useDatabase();

    // will be called when the pattern section edit is made
    const updatePatternSectionData = ({}) => {

    }
}